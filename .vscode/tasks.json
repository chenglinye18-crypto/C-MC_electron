{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build main",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "set -euo pipefail\ncd \"${workspaceFolder}\"\nmake CPPFLAGS='-I/home/ic/trilinos_install/include' CXXFLAGS='-g -O0'\ncp -v lib/main finfet/main"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
